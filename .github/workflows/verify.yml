name: verify

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  verify:
    uses: Project-N-E-K-O/N.E.K.O/.github/workflows/plugin-market-verify.yml@main
    with:
      plugin-id: shell_cmd
