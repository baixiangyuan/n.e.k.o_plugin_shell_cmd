name: release

on:
  push:
    tags:
      - "v*"

jobs:
  release:
    uses: Project-N-E-K-O/N.E.K.O/.github/workflows/plugin-market-release.yml@main
    with:
      plugin-id: shell_cmd
    secrets: inherit
